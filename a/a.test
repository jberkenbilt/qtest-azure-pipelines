#!/usr/bin/env perl
require 5.008;
use warnings;
use strict;

require TestDriver;

my $td = new TestDriver('test');

$td->runtest("command",
	     {$td->COMMAND => "echo potato"},
	     {$td->STRING => "potato\n", $td->EXIT_STATUS => 0},
             $td->NORMALIZE_NEWLINES);

$td->report(1);
